// The calendar only shows issues tagged with one of these content types (the
// `con/` prefix is stripped for display, so these are the bare names). The label
// filter offers exactly these, and an empty selection means "any of them".
export const CONTENT_TAG_NAMES: string[] = ["blog", "newsletter", "socials", "video", "web"];
